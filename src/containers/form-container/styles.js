import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(theme => ({
	formContainer: {
		marginTop: '2em',
		textAlign: 'center',
	},
	formContainer__paperStyle: {
      maxWidth: '330px',
      margin: '2em auto',
   },
   formContainer__tabs: {
      fontWeight: '900',
      textAlign: 'center',
   },
}));